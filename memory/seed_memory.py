from vector_store import build_store

build_store([
    "Urgency messaging increases CTR in seasonal campaigns",
    "High CPC usually means targeting too broad",
    "ROAS under 1.5 requires bid reduction or creative change",
    "Short headlines outperform long ones in cold audiences"
])

print("Memory initialized.")